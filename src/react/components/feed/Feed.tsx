import React from 'react';
import { PhotoCard } from '../photoCard/PhotoCard';
import Grid from '@mui/material/Grid';

const Feed = () => {
  return (
    <Grid>
      <PhotoCard />
    </Grid>
  );
};

export { Feed };

import React, { useState } from 'react';
import { PhotoCard } from '../PhotoCard';
import Grid from '@mui/material/Grid';


const Feed = () => {
  return (
    <Grid>
      <PhotoCard />
    </Grid>
  );
};

export { Feed };
